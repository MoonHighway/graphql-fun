enum Instrument {
  BASS
  DRUMS
  PERCUSSION
  SAMPLER
  SYNTH
}

type Wejay implements Game {
  # paste in final interface fields
  musicians: [Player!]!
  playingMusic: [Player!]!
  faces: [Player!]!
}

type WejayInstructions implements Instructions {
  # paste in final interface fields
  instrument: Instrument
}

extend type Mutation {
  play: Boolean!
  pause: Boolean!
}
